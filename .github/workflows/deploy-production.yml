name: Deploy Production

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: production-deploy
  cancel-in-progress: false

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

jobs:
  deploy-production:
    name: Deploy portfolio-web to Vercel Production
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.11"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Vercel CLI
        run: bun add -g vercel

      - name: Pull Vercel project settings
        run: |
          vercel pull \
            --yes \
            --environment=production \
            --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build portfolio-web locally
        run: bun run build:portfolio

      - name: Deploy to Vercel Production
        id: deploy
        run: |
          url=$(vercel deploy \
            --prod \
            --token=${{ secrets.VERCEL_TOKEN }})
          echo "Production URL: $url"
          echo "url=$url" >> "$GITHUB_OUTPUT"

  deploy-playground-production:
    name: Deploy playground to Vercel Production
    runs-on: ubuntu-latest
    # Skips itself until the second Vercel project exists. Delete once set.
    if: vars.PLAYGROUND_DEPLOY_ENABLED == 'true'

    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_PLAYGROUND }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.11"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Vercel CLI
        run: bun add -g vercel

      - name: Pull Vercel project settings
        run: |
          vercel pull \
            --yes \
            --environment=production \
            --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build playground locally
        run: bun run build:playground

      - name: Deploy to Vercel Production
        run: |
          url=$(vercel deploy \
            --prod \
            --token=${{ secrets.VERCEL_TOKEN }})
          echo "Playground production: $url"
